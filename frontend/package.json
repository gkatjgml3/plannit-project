{
  "name": "planit-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "PLANIT desktop HTML, CSS, and JavaScript frontend",
  "scripts": {
    "build": "node scripts/build.js",
    "preview": "node scripts/serve.js",
    "test": "node --test tests/*.test.js"
  }
}
