# PLANNIT(플랜잇) Codex 개발 기준서

> 이 문서는 `TalkFile_프로그램 개발(1학년)_플랜메이커_플랜잇_1304_김래원.hwp.hwp`의 내용과, 사용자가 직접 요청한 **“다른 프로젝트와 섞지 말 것”**이라는 작업 지시만을 바탕으로 작성한 독립형 개발 명세다.

## 0. Codex가 가장 먼저 지켜야 할 규칙

1. 프로젝트 이름은 **PLANNIT(플랜잇)**이다.
2. 이 문서를 PLANNIT의 제품 요구사항 기준으로 사용한다.
4. 저장소에서 다른 프로젝트의 흔적을 발견하더라도 임의로 합치거나 재사용하지 않는다. PLANNIT에 필요한 파일만 별도로 작성하고, 충돌이 있으면 작업 결과에 명시한다.
5. 원문에 없는 사항을 이미 확정된 요구사항처럼 표현하지 않는다. 이 문서에서 `[확정]`, `[구현 해석]`, `[미정]`을 다음 뜻으로 사용한다.
   - `[확정]`: 첨부 HWP 기획서에 명시된 내용
   - `[구현 해석]`: 기획서의 흐름을 실제 소프트웨어로 만들기 위해 이 문서에서 구체화한 최소 결정
   - `[미정]`: 기획서에서 정하지 않았으며 구현 전에 선택하거나 임시 구현해야 하는 사항
6. `[미정]` 때문에 전체 작업을 멈추지 않는다. 교체 가능한 인터페이스와 단순한 기본 구현으로 먼저 진행하고, 선택 사항을 문서에 기록한다.
7. AI가 읽지 못한 내용이나 이미지에 없는 정보는 만들어 내지 않는다. 비어 있거나 불확실한 값은 사용자에게 확인·수정할 수 있게 표시한다.

## 1. 프로젝트 정의

### 1.1 기본 정보

- `[확정]` 프로젝트명: PLANNIT(플랜잇)
- `[확정]` 출전 맥락: 2026 소프트웨어 챌린지
- `[확정]` 참가 주제: 인공지능과 함께 해결하기
- `[확정]` 제품 형태: 스마트폰과 노트북에서 사용할 수 있는 AI 기반 캘린더·일정 관리 서비스
- `[확정]` 핵심 사용자: 학생
- `[확정]` 확장 사용자: 직장인, 일정이 많은 사람, 일정 관리가 어려운 사람

### 1.2 한 문장 설명

학교 안내문·학습지·시험 자료 등의 이미지를 올리면 AI가 일정 정보를 추출하고, 사용자가 확인·수정한 뒤 캘린더·알림·그룹 공유로 관리할 수 있게 하는 서비스다.

### 1.3 해결하려는 문제

- `[확정]` 제목, 날짜, 시간, 장소, 알림을 매번 직접 입력해야 하는 피로
- `[확정]` 긴 문장이나 안내 자료에서 일정 정보를 찾아 옮기는 불편
- `[확정]` 여러 일정의 충돌과 이동 시간을 직접 확인해야 하는 문제
- `[확정]` 할 일과 일정이 분리되어 우선순위와 실행 시간을 스스로 정해야 하는 문제
- `[확정]` 이메일·메신저·문자 등에 흩어진 정보로 인한 맥락 단절
- `[확정]` 시험, 수행평가, 과제, 학사 공지를 놓치는 문제
- `[확정]` 조별 과제·학급·동아리처럼 여러 사람이 함께 관리해야 하는 일정의 공유 문제

### 1.4 기대 효과

- `[확정]` 반복적인 일정 입력과 수정 시간 절약
- `[확정]` 시험·수행평가·과제 일정을 한눈에 확인
- `[확정]` 중요한 일정 누락 예방
- `[확정]` 친구들과 조별 활동·학급 일정 공유
- `[확정]` 알림을 통한 학습 계획 수립과 실천 지원
- `[확정]` 일정 관리 실수와 학업 스트레스 감소

## 2. 제품 범위

### 2.1 핵심 사용자 흐름

```text
안내문 촬영 또는 이미지 선택
  → 이미지 업로드
  → AI가 날짜와 일정 내용 분석
  → 여러 일정 후보 생성
  → 사용자가 원본과 결과를 확인·수정
  → 캘린더에 저장
  → 일정 알림
  → 필요하면 반·동아리·스터디·조별 그룹에 공유
```

`[구현 해석]` 기획서에는 “자동 등록된 일정을 확인하고 수정”한다고 되어 있다. 잘못 인식된 일정이 바로 확정되는 문제를 줄이기 위해 실제 구현은 **분석 결과를 편집 가능한 임시 일정으로 보여 준 뒤 사용자가 저장을 확정하는 방식**을 기본으로 한다.

### 2.2 우선순위

#### P0 — 반드시 먼저 완성할 MVP

1. 이미지 선택·업로드
2. AI 이미지 분석
3. 한 이미지에서 한 개 이상의 일정 추출
4. 추출 결과 확인·수정
5. 일정 저장
6. 캘린더에서 일정 확인

#### P1 — 핵심 확장

1. 일정별 알림
2. 반·동아리·스터디·조별 과제 그룹
3. 그룹 일정 공유와 최신 변경 내용 반영
4. 장소·준비물·메모 등 세부 정보 표시

#### P2 — 고급 계획 기능

1. 반복 일정 처리
2. 시험 대비·과제 준비 일정을 목표 기한에 맞춰 자동 분배
3. 일정 충돌 확인
4. 이동 시간을 고려한 충돌 경고
5. 빈 시간과 중요도를 고려한 실행 시간 제안
6. 이동 중·쉬는 시간 등 짧은 세션에 맞춘 반복 확인 알림

### 2.3 현재 범위로 확정하지 않는 것

- `[확정 아님]` 이메일·카카오톡·문자 서비스와 직접 연동하는 기능. 기획서에서는 흩어진 정보가 문제라고 설명했지만, 구체적인 연동 기능은 제시하지 않았다.
- `[확정 아님]` 학교 시스템, 외부 캘린더 또는 학사 시스템과의 동기화
- `[확정 아님]` 결제, 광고, 구독
- `[확정 아님]` 교사용 관리자 시스템
- `[확정 아님]` 특정 AI 업체, 특정 데이터베이스, 특정 배포 서비스

## 3. 기능 요구사항

### FR-01. 이미지 입력

- `[확정]` 사용자는 학교에서 받은 학습지, 공지문, 시험 안내 자료, 수행평가 안내문 등의 이미지를 업로드할 수 있어야 한다.
- `[확정]` 스마트폰과 노트북에서 사용할 수 있어야 한다.
- `[구현 해석]` 업로드 전에 선택한 이미지의 미리보기를 제공한다.
- `[구현 해석]` 지원하지 않는 파일, 손상된 파일, 읽기 어려운 이미지는 이해할 수 있는 오류 메시지와 재시도 방법을 보여 준다.

### FR-02. AI 일정 분석

- `[확정]` AI는 이미지 속 날짜와 일정 내용을 분석해야 한다.
- `[확정]` 한 이미지에 여러 날짜 또는 여러 일정이 있으면 한 번에 여러 일정으로 처리해야 한다.
- `[확정]` 이미지에 존재하는 경우 제목, 날짜, 시간, 장소, 준비물, 상세 내용 또는 메모를 추출해야 한다.
- `[확정]` 반복 수업·과제·시험 일정도 처리 대상으로 고려한다.
- `[구현 해석]` 인식하지 못한 필드는 빈 값으로 두고 경고를 표시한다.
- `[구현 해석]` AI 원문 응답을 그대로 저장 모델로 사용하지 않고, 일정 후보 형식으로 변환·검증한다.

### FR-03. 분석 결과 확인과 수정

- `[확정]` 사용자는 자동 생성된 일정을 확인하고 수정할 수 있어야 한다.
- `[구현 해석]` 원본 이미지와 추출된 일정 목록을 같은 작업 흐름에서 비교할 수 있어야 한다.
- `[구현 해석]` 여러 일정은 각각 편집하거나 저장 대상에서 제외할 수 있어야 한다.
- `[구현 해석]` 날짜가 없거나 해석이 모호한 일정은 저장 전에 눈에 띄게 표시한다.

### FR-04. 일정 저장과 캘린더

- `[확정]` 확인된 일정은 캘린더에 등록되어야 한다.
- `[확정]` 저장된 일정은 캘린더에서 직관적으로 확인할 수 있어야 한다.
- `[확정]` 사용자는 저장된 일정을 수정할 수 있어야 한다.
- `[구현 해석]` 일정 상세 화면에서 제목, 날짜·시간, 장소, 준비물, 메모, 반복, 알림, 공유 그룹을 확인한다.
- `[미정]` 월간·주간·일간 보기 중 어떤 보기를 제공할지는 정해져 있지 않다. MVP는 월간 보기와 날짜별 목록을 기본으로 삼을 수 있다.
- `[미정]` 일정 삭제 기능은 기획서에 직접 명시되지 않았다. 완전한 일정 관리에 필요하면 MVP 보조 기능으로 추가하되 요구사항 추가 사실을 기록한다.

### FR-05. 알림

- `[확정]` 중요 일정의 날짜가 가까워지면 알림을 받을 수 있어야 한다.
- `[확정]` 일정별 알림 기능을 제공해야 한다.
- `[확정]` 이동 중이나 쉬는 시간처럼 짧은 시간 단위로 일정을 반복 확인할 수 있는 알림을 고려한다.
- `[구현 해석]` MVP에서는 일정별 알림 시각을 저장하고 앱 안에서 예정 알림을 확인하는 기능부터 구현할 수 있다.
- `[미정]` 브라우저 푸시, 이메일, 문자, 모바일 네이티브 알림 중 실제 전달 수단은 정해져 있지 않다.

### FR-06. 그룹과 일정 공유

- `[확정]` 사용자는 반, 동아리, 스터디, 조별 과제 등의 단체 방을 만들 수 있어야 한다.
- `[확정]` 그룹에 업로드되거나 공유된 일정은 구성원에게 공유되어야 한다.
- `[확정]` 구성원은 최신 일정 정보를 확인할 수 있어야 한다.
- `[확정]` 일정 공유 과정에서 개인정보와 일정 정보 보호를 고려해야 한다.
- `[구현 해석]` 최소한 그룹 소유자와 일반 구성원을 구분하고, 그룹 구성원만 그룹 일정을 볼 수 있게 한다.
- `[구현 해석]` 개인 일정과 그룹 일정을 화면에서 구분한다.
- `[미정]` 초대 링크, 초대 코드, 직접 추가 중 그룹 가입 방식은 정해져 있지 않다.

### FR-07. 일정 충돌과 이동 시간

- `[확정]` 여러 일정이 겹치는 문제와 이동 시간을 고려하지 못해 늦는 문제를 해결 대상으로 삼는다.
- `[구현 해석]` 새 일정을 저장할 때 기존 일정과 시간이 겹치면 경고한다.
- `[미정]` 실제 교통·지도 데이터 연동은 정해져 있지 않다. 외부 지도 연동 전에는 사용자가 입력한 이동 여유 시간으로 단순 경고할 수 있다.

### FR-08. 준비 일정 자동 분배

- `[확정]` 반복 일정이나 시험 대비 일정은 목표 기한에 맞춰 자동으로 분배하는 기능을 고려한다.
- `[확정]` 사용자가 언제 무엇을 해야 하는지 직관적으로 확인할 수 있어야 한다.
- `[구현 해석]` P2에서는 목표일, 필요한 총 준비 시간, 세션 길이, 사용 가능한 시간대를 입력받아 준비 세션을 제안하는 방식으로 시작할 수 있다.
- `[미정]` 자동 분배 알고리즘과 우선순위 규칙은 기획서에 정의되어 있지 않다.

## 4. 화면 요구사항

### 4.1 MVP 화면

1. **캘린더 홈**
   - 날짜별 일정 요약
   - 개인 일정과 그룹 일정 구분
   - 이미지 업로드로 바로 이동하는 주요 버튼
2. **이미지 업로드**
   - 촬영 또는 파일 선택
   - 이미지 미리보기
   - 분석 시작
3. **분석 중 화면**
   - 진행 상태
   - 취소 또는 재시도 안내
4. **분석 결과 확인·수정**
   - 원본 이미지
   - 여러 일정 후보 목록
   - 일정별 필드 편집
   - 모호하거나 누락된 값 표시
   - 선택한 일정 일괄 저장
5. **일정 상세·수정**
   - 제목, 날짜·시간, 장소, 준비물, 메모, 반복, 알림, 공유 그룹
6. **그룹 목록·상세**
   - 그룹 만들기·가입
   - 구성원과 공유 일정 확인

### 4.2 반드시 구분할 UI 상태

- 처음 사용하는 상태
- 이미지 선택 완료
- 분석 중
- 분석 성공
- 여러 일정 추출 성공
- 일부 필드만 추출된 상태
- 날짜가 모호한 상태
- 일정이 발견되지 않은 상태
- 이미지 화질이 낮은 상태
- 분석 실패
- 저장 성공·실패
- 네트워크 오류와 재시도
- 그룹 접근 권한 없음

### 4.3 모바일 우선 원칙

- `[확정]` 스마트폰에서 사용 가능해야 한다.
- `[구현 해석]` 핵심 흐름은 한 손 조작을 고려하고, 주요 버튼은 화면 하단에서도 쉽게 누를 수 있게 한다.
- `[구현 해석]` 긴 안내문 이미지는 확대·축소해 원문을 확인할 수 있게 한다.
- `[구현 해석]` 한 화면에 너무 많은 필드를 강제로 배치하지 않고 일정 카드 단위로 나눈다.

## 5. 데이터 모델 초안

이 절은 기획서 내용을 구현 가능한 형태로 옮긴 `[구현 해석]`이며, 필드명과 저장 방식은 확정 기술에 맞게 조정할 수 있다.

### 5.1 Schedule

```text
id
ownerId
groupId?              # 없으면 개인 일정
title
date
startTime?
endTime?
location?
materials[]
memo?
recurrence?
reminderAt?
sourceImageId?
createdAt
updatedAt
```

### 5.2 ImageAnalysis

```text
id
uploaderId
imageReference
status                # pending | processing | needs_review | failed | confirmed
scheduleCandidates[]
warnings[]
createdAt
```

### 5.3 Group

```text
id
name
type                  # class | club | study | team | other
ownerId
createdAt
```

### 5.4 GroupMember

```text
groupId
userId
role                  # owner | member
joinedAt
```

### 5.5 Notification

```text
id
scheduleId
userId
notifyAt
status
```

### 5.6 최소 사용자 식별

그룹 공유와 개인정보 보호를 구현하려면 사용자를 구분할 방법이 필요하다. 인증 방식은 `[미정]`이다. MVP에서는 실제 인증을 붙이기 전에도 사용자 식별 인터페이스를 분리해 두고, 그룹 데이터 접근 시 구성원 여부를 반드시 검사한다.

## 6. AI 분석 계약 초안

특정 AI 업체는 `[미정]`이다. AI 호출부는 교체 가능한 어댑터로 분리한다. 다음 JSON은 구현을 시작하기 위한 `[구현 해석]`이며 원문이 지정한 포맷은 아니다.

```json
{
  "schedules": [
    {
      "title": "수행평가 발표",
      "date": "2026-09-15",
      "startTime": "10:00",
      "endTime": null,
      "location": "교실",
      "materials": ["발표 자료"],
      "memo": "이미지에서 확인된 세부 내용",
      "recurrence": null,
      "reminder": null,
      "uncertainFields": []
    }
  ],
  "warnings": []
}
```

### 6.1 검증 규칙

- `schedules`는 배열이어야 한다.
- 일정이 없으면 빈 배열과 사용자용 경고를 반환한다.
- `title`과 `date`가 없거나 모호하면 자동 확정하지 않고 검토 대상으로 표시한다.
- 날짜와 시간은 실제 달력에서 유효해야 한다.
- 이미지에 없는 장소·준비물·내용을 추측해 채우지 않는다.
- 한 이미지의 여러 날짜는 각각 독립된 일정 후보가 되어야 한다.
- AI 오류, JSON 오류, 시간 초과는 일반 서버 오류와 구분해 재시도할 수 있게 한다.
- 원본 이미지와 AI의 원문 응답에 개인정보가 있을 수 있으므로 불필요하게 화면·로그에 노출하지 않는다.

## 7. 기술 기준

### 7.1 기획서에서 확정된 기술·도구

- `[확정]` HTML: 사용자 화면 구성
- `[확정]` Java: 일정 처리와 기능 구현
- `[확정]` Visual Studio Code: 프로그램 개발과 코드 작성
- `[확정]` IntelliJ IDEA: Java 코드 작성과 기능 구현
- `[확정]` GitHub: 프로젝트 파일 관리
- `[확정]` AI 이미지 분석 기술: 사진 속 일정 정보 추출

### 7.2 구현을 위해 허용되는 최소 보완

- `[구현 해석]` HTML 화면의 디자인과 상호작용을 위해 CSS와 JavaScript를 사용할 수 있다.
- `[구현 해석]` 프론트엔드, Java 서버, AI 분석기, 데이터 저장소를 서로 교체 가능한 경계로 분리한다.
- `[구현 해석]` AI 비밀키나 개인정보는 프론트엔드 코드와 저장소에 넣지 않는다.
- `[구현 해석]` 저장·공유·분석 실패가 전체 화면을 깨뜨리지 않도록 사용자용 오류와 개발용 오류를 구분한다.

### 7.3 아직 정해지지 않은 기술

- Java 웹 프레임워크와 빌드 도구
- 데이터베이스 종류와 운영 방식
- AI 제공 업체와 모델
- 인증 방식
- 알림 전달 채널
- 실시간 공유 구현 방식
- 이미지 저장 위치와 보관 기간
- 배포 플랫폼과 도메인
- 지원 이미지 형식·용량 제한

Codex는 저장소에 이미 **PLANNIT용으로 확인되는** 기술 구성이 있으면 그 구성을 우선 검토한다. 다른 프로젝트에서 가져온 구성은 사용하지 않는다. 구성이 없다면 단순하고 교체 가능한 구조로 시작하고 선택 이유를 PLANNIT 문서에 남긴다.

## 8. 권장 내부 API 초안

다음은 프론트엔드와 Java 서버의 역할을 나누기 위한 `[구현 해석]`이다. 실제 경로는 변경할 수 있지만 기능 경계는 유지한다.

```text
POST   /api/analyses                 이미지 업로드 및 분석 시작
GET    /api/analyses/{id}            분석 상태와 일정 후보 조회

POST   /api/schedules/batch          확인한 여러 일정 일괄 저장
GET    /api/schedules                기간별 일정 조회
GET    /api/schedules/{id}           일정 상세 조회
PUT    /api/schedules/{id}           일정 수정

POST   /api/groups                   그룹 생성
GET    /api/groups                   내 그룹 조회
GET    /api/groups/{id}/schedules    그룹 일정 조회
POST   /api/groups/{id}/schedules    그룹 일정 공유

GET    /api/notifications            예정 알림 조회
```

API 응답은 성공 데이터, 사용자에게 보여 줄 메시지, 개발용 오류 코드를 구분한다. 그룹 API는 요청 사용자가 해당 그룹 구성원인지 확인해야 한다.

## 9. 구현 순서

### 단계 0. 프로젝트 격리와 기준 확정

- 이 문서를 기준으로 PLANNIT 파일과 다른 프로젝트 흔적을 구분한다.
- 다른 프로젝트 파일을 삭제하거나 덮어쓰지 않는다.
- `[미정]` 기술 선택을 목록화하고, 당장 필요한 항목만 임시 결정한다.

### 단계 1. 기본 화면과 일정 모델

- 모바일 우선 기본 레이아웃
- 캘린더 홈
- 일정 모델과 저장소 경계
- 일정 생성·조회·수정

### 단계 2. 이미지 업로드

- 파일 선택 또는 촬영 입력
- 이미지 미리보기
- 업로드 오류 처리
- 분석 상태 UI

### 단계 3. AI 분석과 확인 화면

- AI 분석 어댑터
- 구조화된 여러 일정 후보
- 필드 검증
- 원본과 결과 비교
- 수정·선택 후 일괄 저장

### 단계 4. 캘린더와 알림

- 날짜별 일정 표시
- 상세·수정
- 일정별 알림 설정
- 예정 알림 확인

### 단계 5. 그룹 공유

- 그룹 생성·가입
- 구성원 확인
- 개인·그룹 일정 구분
- 그룹 일정 공유와 변경 반영
- 권한·개인정보 보호 테스트

### 단계 6. 고급 계획 기능

- 반복 일정
- 충돌 경고
- 이동 여유 시간
- 시험·과제 준비 세션 자동 분배
- 짧은 세션 단위 알림

### 단계 7. 통합 테스트와 발표 준비

- 전체 흐름 통합
- 오류 수정
- 모바일 사용성 개선
- 최종 시나리오 테스트
- 발표용 데모 데이터와 실행 방법 정리

## 10. 원문 개발 일정

아래 일정은 HWP 기획서의 계획이며, 실제 구현 완료 상태를 뜻하지 않는다.

| 기간 | 원문 단계 | 작업 |
|---|---|---|
| 5/15~5/31 | 서비스 기획 및 화면 구성 | 아이디어·기능 구체화, UI 설계, 개발 환경 조사 |
| 6/1~7/31 | 기본 기능 개발 | 기본 화면, 일정 저장·수정, 사진 업로드 |
| 8/1~9/30 | AI 일정 분석 기능 | 이미지 일정 인식, 자동 캘린더 등록, 메모 |
| 10/1~10/31 | 캘린더 및 알림 | 캘린더, 알림, 일정 공유 |
| 11/1~11/30 | 기능 통합 및 테스트 | 기능 통합, 오류 수정, 사용자 편의성 개선 |
| 12/3~12/18 | 최종 테스트 및 발표 준비 | 최종 점검, 흐름 테스트, 발표 자료, 리허설 |

## 11. 테스트 시나리오

### TS-01. 수행평가 안내문 한 건

1. 사용자가 선명한 수행평가 안내문 이미지를 올린다.
2. AI가 제목과 날짜, 이미지에 있는 세부 정보를 추출한다.
3. 사용자가 결과를 수정해 저장한다.
4. 해당 날짜의 캘린더에 일정이 표시된다.

### TS-02. 한 이미지에 여러 일정

1. 서로 다른 날짜가 세 개 있는 안내문을 올린다.
2. 세 개의 독립된 일정 후보가 표시된다.
3. 사용자가 일부만 선택하거나 각각 수정할 수 있다.
4. 선택한 일정만 캘린더에 저장된다.

### TS-03. 낮은 화질 또는 복잡한 안내문

1. 흐리거나 복잡한 이미지를 올린다.
2. 시스템은 잘못된 값을 확정적으로 만들지 않는다.
3. 누락·모호한 필드를 표시하고 재촬영 또는 직접 수정을 안내한다.

### TS-04. 날짜가 없는 이미지

1. 일정 내용은 있지만 날짜가 없는 이미지를 올린다.
2. 날짜가 필요하다는 경고와 편집 화면을 제공한다.
3. 사용자가 날짜를 입력하기 전에는 최종 저장하지 않는다.

### TS-05. 알림

1. 일정에 알림 시각을 설정한다.
2. 예정 알림 목록에 나타난다.
3. 일정 시간을 수정하면 알림도 일관되게 갱신된다.

### TS-06. 그룹 공유와 권한

1. 사용자가 그룹을 만들고 일정을 공유한다.
2. 구성원은 최신 일정을 볼 수 있다.
3. 구성원이 아닌 사용자는 그룹 일정을 볼 수 없다.
4. 개인 일정은 공유 대상으로 선택하지 않으면 그룹에 노출되지 않는다.

### TS-07. 충돌 일정

1. 기존 일정과 시간이 겹치는 새 일정을 만든다.
2. 저장 전에 충돌 경고가 표시된다.
3. 사용자는 시간을 수정하거나 경고를 확인한 뒤 유지할 수 있다.

### TS-08. 준비 일정 자동 분배

1. 시험 목표일과 준비 시간을 입력한다.
2. 시스템이 여러 준비 세션을 제안한다.
3. 사용자가 제안을 확인·수정한 뒤 캘린더에 저장한다.

## 12. 단계별 완료 기준

### MVP 완료 기준

- 스마트폰 크기 화면에서 이미지 업로드부터 일정 저장까지 중단 없이 수행된다.
- 한 이미지의 여러 날짜가 여러 일정 후보로 분리된다.
- 사용자가 AI 결과를 저장 전에 확인하고 수정할 수 있다.
- 저장한 일정이 캘린더에 나타나고 다시 수정할 수 있다.
- 낮은 화질, 일정 없음, 분석 실패, 저장 실패 상태가 구분된다.
- AI가 이미지에 없는 정보를 사실처럼 만들어 저장하지 않는다.
- 핵심 흐름에 자동화된 테스트 또는 재현 가능한 수동 테스트 절차가 있다.

### 알림 완료 기준

- 일정별 알림 시각을 설정·변경할 수 있다.
- 일정 변경과 알림 상태가 서로 어긋나지 않는다.
- 실제 전달 채널이 아직 없으면 앱 안에서 알림 예정 상태를 검증할 수 있다.

### 그룹 공유 완료 기준

- 그룹 생성·가입·구성원 확인이 가능하다.
- 그룹 일정과 개인 일정이 구분된다.
- 구성원만 그룹 일정에 접근할 수 있다.
- 일정 변경이 다른 구성원의 최신 조회 결과에 반영된다.

### 고급 기능 완료 기준

- 충돌 경고가 겹치는 일정을 일관되게 탐지한다.
- 자동 분배 결과를 사용자가 확정 전에 수정할 수 있다.
- 알고리즘이 제안한 일정과 사용자가 직접 만든 일정을 구분할 수 있다.

## 13. 예상 문제와 대응 원칙

### 사진 화질과 촬영 환경

- `[확정]` 화질과 촬영 환경에 따라 인식 정확도가 낮아질 수 있다.
- 대응: 업로드 전 미리보기, 재촬영 안내, 모호한 필드 표시, 직접 수정 경로를 제공한다.

### 손글씨와 복잡한 안내문

- `[확정]` 잘못 분석할 가능성이 있다.
- 대응: 여러 일정 후보를 사용자에게 보여 주고, AI 결과를 최종 사실로 취급하지 않는다.

### 다양한 안내문 형식

- `[확정]` 추가 데이터 학습과 기능 개선이 필요할 수 있다.
- 대응: AI 제공 업체에 종속되지 않는 분석 인터페이스와 고정 테스트 이미지를 둔다.

### 개인정보와 공유 일정 보호

- `[확정]` 일정 공유 시 개인정보와 일정 정보 보호 문제가 생길 수 있다.
- 대응: 그룹 구성원 확인, 개인·공유 일정 분리, 불필요한 이미지·분석 데이터 노출 방지를 기본으로 한다.

## 14. Codex 작업 보고 형식

각 구현 작업을 마칠 때 다음을 짧게 보고한다.

1. 이번에 완성한 PLANNIT 기능
2. 변경한 파일
3. 실행한 테스트와 결과
4. 아직 `[미정]`이거나 임시 구현인 부분
5. 다음 우선순위 작업

다른 프로젝트의 코드나 요구사항을 사용했다는 표현이 나오면 작업을 중단하고 PLANNIT 범위로 다시 분리한다.

## 15. 최종 제품 원칙

PLANNIT은 단순한 캘린더가 아니라 다음 흐름을 짧고 안전하게 연결하는 제품이어야 한다.

```text
사진 한 장
  → 여러 일정 추출
  → 사람이 확인·수정
  → 캘린더 등록
  → 알림과 그룹 공유
  → 필요할 때 충돌 확인과 준비 계획 분배
```

기능 수보다 먼저 **이미지 업로드 → 분석 → 확인·수정 → 캘린더 저장**의 핵심 흐름을 안정적으로 완성한다.
