{
  "name": "plannit-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "PLANNIT desktop HTML, CSS, and JavaScript frontend",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js"
  }
}
